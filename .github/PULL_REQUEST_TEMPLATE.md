## What

<!-- One paragraph: what changes and why. Link the issue. -->

## Checklist

- [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` pass
- [ ] Nothing city-specific hardcoded (names, colours, providers come from the city config)
- [ ] Copy added in both `es` and `en` (`src/lib/i18n/dict.ts`)
- [ ] Map screens keep the map-first rules (`UX-AUDIT.md`)
- [ ] Screenshots updated if a screen changed (`pnpm screenshots…`)
